<div class="row">
    <div class="question full large-8 medium-8 small-10 large-centered medium-centered small-centered columns text-center">{{question}}</div>
</div>

{{#each choices}}
<div class="large-8 medium-8 small-10 large-centered medium-centered small-centered columns text-center">
    <button id="tag" class={{tag}}>{{label}}</button>
</div>
{{/each}}

